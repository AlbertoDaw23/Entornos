"INFORMACION IMPORTANTE"

Esto se trata de una prueba de github y estoy tratando con los repositorios de la aplicación


"SEGUNDA ACTUALIZACION"


Añadimos el metodo para ver la hora y la fecha en el programa principal.java

"TERCERA ACTUALIZACION"

En esta rama, se ha creado la clase `Usuario` con tres atributos públicos: `nombre`, `apellidos` e `email`. también, he añadido en el programa principal un metodo que solicita al usuario la información para cada elemento de un array de tipo `Usuario` según la cantidad proporcionada por el usuario. Luego, muestra el contenido del array en la consola.